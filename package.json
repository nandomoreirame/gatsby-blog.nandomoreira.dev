{
  "private": true,
  "name": "site",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npm run clean && gatsby build",
    "dev": "gatsby develop -H 0.0.0.0 -p 8000",
    "deploy": "npm run build && netlify deploy --dir=public",
    "release": "npm run deploy --prod",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "@narative/gatsby-theme-novela": "^0.*",
    "gatsby": "^2.13.41",
    "gatsby-plugin-manifest": "^2.2.4",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  }
}
